<h1>Your Shopping Cart</h1>

<% if @cart.products_carts.empty? %>
  <p>Your cart is empty.</p>
<% else %>
  <table class="cart-table">
    <thead>
      <tr>
        <th>Product</th>
        <th>Quantity</th>
        <th>Price</th>
        <th>Total</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @cart.products_carts.each do |product_cart| %>
        <% product = product_cart.product %>
        <tr>
          <td><%= product.product_name %></td>
          <td><%= product_cart.quantity %></td>
          <td>$<%= product.price %></td>
          <td>$<%= product.price * product_cart.quantity %></td>
          <td>
            <%= button_to 'Remove', remove_from_cart_path(product_cart.id), method: :delete, class: 'btn btn-secondary' %>
            <%= form_with url: update_cart_path(product_cart.id), method: :patch, local: true do |form| %>
              <%= form.number_field :quantity, value: product_cart.quantity, min: 1, class: 'cart-quantity-field' %>
              <%= form.submit 'Update', class: 'btn btn-primary' %>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <p class="cart-total">Total Price: $<%= @cart.total_price %></p>
  <div class="cart-actions">
    <%= link_to 'Checkout', new_order_path, class: 'btn btn-primary' %>
  </div>
<% end %>
